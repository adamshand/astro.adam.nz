---
// export interface Props {
//   id: string
//   title?: string
//   size?: string
// }

const { id, title, size = 48 } = Astro.props
const moji = `https://openmoji.org/data/color/svg/${id}.svg` // is this bad?  it caches right?
// const moji = `./src/components/openmoji/${id}.svg` // TODO want to use @ alias, how?
// import moji from `@/components/openmoji/${id}.svg` // no wookie

// TODO Update to pull emoji from openmoji website directly?
// <Picture
//   alt={title ? title : ''}
//   aspectRatio="1:1"
//   src={`https://openmoji.org/data/color/svg/${id}.svg`}
//   widths={[16, 32, 64]}
// />
---

<img 
  class="openmoji"
  alt={title ? title : ''}
  title={title ? title : ''}
  width={size}
  height={size}
  src={moji}
/>

<style>
  /* img a:hover {
    padding: 5px;
    border: 1px solid silver;
  } */
</style>
